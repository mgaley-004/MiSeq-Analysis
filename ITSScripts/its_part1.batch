set.dir(output=., input=.)
make.file(inputdir=., prefix=$PREFIX, type=gz)
make.contigs(file=current, trimoverlap=F, gapextend=0)
summary.seqs(fasta=current)
screen.seqs(fasta=current, group=current, maxambig=0, maxhomop=9)
summary.seqs(fasta=current)
unique.seqs(fasta=current)
count.seqs(name=current, group=current)
summary.seqs(fasta=current, count=current)
rename.file(fasta=current, count=current, name=current, group=current, prefix=$INTERPREFIX)
quit()